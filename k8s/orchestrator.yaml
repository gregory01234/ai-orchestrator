apiVersion: v1
kind: Pod
metadata:
  name: orchestrator
  labels:
    app: orchestrator
spec:
  containers:
    - name: orchestrator
      image: alpine:latest
      command: ["sh", "-c", "echo orchestrator running && sleep 3600"]
